[pytest]
markers =
    slow: long-running integration tests
    smoke: smoke-style deployment and relation checks
    abort_on_fail: stop the session when this test fails
    incremental: run related tests in sequence
